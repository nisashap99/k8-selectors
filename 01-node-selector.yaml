apiVersion: v1
kind: Pod
metadata:
  name: node-Selector
  labels:
    purpose: "Selector-demo"
spec:
  nodeSelector:
    topology.kubernetes.io/zone: us-east-1c
  containers:
  - name: nginx
    image: nginx